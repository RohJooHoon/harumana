# HaruManna (하루만나)

**HaruManna**는 매일의 말씀을 묵상하고(QT), 공동체와 함께 기도 제목을 나누는 영적 성장을 위한 플러터(Flutter) 애플리케이션입니다.

![Flutter](https://img.shields.io/badge/Flutter-%2302569B.svg?style=for-the-badge&logo=Flutter&logoColor=white)
![Dart](https://img.shields.io/badge/Dart-0175C2?style=for-the-badge&logo=dart&logoColor=white)
![Firebase](https://img.shields.io/badge/Firebase-039BE5?style=for-the-badge&logo=Firebase&logoColor=white)

## 📱 주요 기능 (Features)

### 1. 홈 (Home)
- **오늘의 말씀**: 매일 제공되는 성경 본문과 목사님의 묵상 포인트를 확인할 수 있습니다.
- **연속 묵상(Streak)**: 묵상을 꾸준히 이어갈 수 있도록 연속 일수를 시각적으로 보여줍니다.

### 2. QT (Quiet Time)
- **묵상 기록**: 제목, 묵상 내용, 적용점, 기도를 체계적으로 기록할 수 있습니다.
- **AI 묵상 가이드**: 구글 Gemini AI를 활용하여 본문에 대한 배경 지식, 묵상 질문, 적용점을 제안받을 수 있습니다.
- **캘린더 뷰**: 한 달간의 묵상 기록 현황을 한눈에 파악할 수 있습니다 (All/Me/Group 상태 표시).

### 3. 기도 (Prayer)
- **기도 나눔**: 중보기도와 1:1 기도 제목을 나누는 공간입니다.
- **AI 기도문 다듬기**: 작성한 기도 제목을 AI가 더 정중하고 은혜로운 문체로 다듬어줍니다.
- **아멘(Amen) 인터랙션**: 다른 사람의 기도에 '아멘' 버튼을 눌러 마음을 전할 수 있습니다.

## 🛠 기술 스택 (Tech Stack)

- **Framework**: Flutter (Dart)
- **State Management**: Provider
- **Backend / Services**:
    - Firebase Core (App Initialization)
    - Google Generative AI (Gemini API)
- **UI Libraries**:
    - `lucide_icons`: 모던한 아이콘 시스템
    - `intl`: 날짜 및 시간 포맷팅

## 🚀 시작하기 (Getting Started)

### 전제 조건 (Prerequisites)
- Flutter SDK가 설치되어 있어야 합니다.
- iOS 시뮬레이터 또는 Android 에뮬레이터가 필요합니다.

### 설치 (Installation)

1. 저장소를 클론합니다.
   ```bash
   git clone https://github.com/RohJooHoon/harumana.git
   cd harumana
   ```

2. 패키지를 설치합니다.
   ```bash
   flutter pub get
   ```

3. (선택사항) Gemini API 키 설정
   - `lib/services/gemini_service.dart` 파일에 API 키를 설정하거나 빌드 시 주입해야 AI 기능을 온전히 사용할 수 있습니다.

### 실행 (Run)

편의를 위해 제공된 실행 스크립트를 사용할 수 있습니다.

**iOS 시뮬레이터 실행:**
```bash
./run-ios.js
```

**Android 에뮬레이터 실행:**
```bash
./run-android.js
```

또는 표준 Flutter 명령어를 사용할 수 있습니다.
```bash
flutter run
```

## 📂 프로젝트 구조 (Project Structure)

```
lib/
├── data/          # Mock Data 및 상수
├── models/        # 데이터 모델 (User, QTLog, PrayerRequest 등)
├── providers/     # 상태 관리 (AppProvider)
├── screens/       # 주요 화면 (Home, QT, Prayer)
├── services/      # 외부 서비스 (Gemini AI 등)
├── theme/         # 앱 테마 및 스타일 정의
├── widgets/       # 재사용 가능한 UI 컴포넌트
└── main.dart      # 앱 진입점
```

## 🎨 디자인 및 개발 가이드라인 (Design & Development Guidelines)

### 1. 페이지 전환 (Navigation)
- **상세/작성 페이지 이동**: 화면 모드 변경(`State` 변수 사용)이 아닌 **`Navigator.push`**를 사용하여 새로운 페이지 스택으로 이동해야 합니다.
  - 이는 iOS/Android의 기본 네이티브 전환 효과(슬라이드 등)를 유지하고, 뒤로 가기 제스처와 아이콘(`<`)의 일관성을 보장하기 위함입니다.
  - 예시: 리스트 아이템 클릭 시, 작성 버튼 클릭 시 `MaterialPageRoute`를 통해 이동.

### 2. 디자인 시스템 (Color System)
- **색상 사용**: 하드코딩된 색상(`Color(0xFF...)` 또는 `Colors.blue`) 사용을 지양하고, 반드시 **`lib/theme/app_theme.dart`**에 정의된 `AppTheme` 클래스의 색상을 사용해야 합니다.
  - `AppTheme.primary`, `AppTheme.secondary`, `AppTheme.point`, `AppTheme.primaryGradient` 등을 사용하세요.

### 3. UX/UI 동작 (Behavior)
- **키보드 닫기**: 텍스트 입력 필드가 있는 모든 화면에서 **입력 필드 외 영역을 터치하면 키보드가 닫혀야 합니다.**
  - `main.dart`의 `MaterialApp` `builder` 속성에서 `GestureDetector`를 사용하여 전역적으로 처리되어 있으므로, 개별 스크린에서 별도로 구현할 필요는 없으나 이 동작이 유지되도록 유의해야 합니다.

---
Running with ❤️ by **HaruManna Team**